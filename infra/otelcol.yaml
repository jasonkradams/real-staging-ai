receivers:
  otlp:
    protocols:
      http:
      grpc:
exporters:
  debug: {}
processors:
  batch: {}
service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters: [debug]
