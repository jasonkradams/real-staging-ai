# Auth0 Configuration
# Obtain these values from your Auth0 Dashboard (https://manage.auth0.com)
# Create a "Regular Web Application" in Auth0 to get these credentials

# Auth0 domain (e.g., dev-sleeping-pandas.us.auth0.com)
AUTH0_DOMAIN=dev-sleeping-pandas.us.auth0.com

# Auth0 client ID
AUTH0_CLIENT_ID=your_auth0_client_id

# Auth0 client secret
AUTH0_CLIENT_SECRET=your_auth0_client_secret

# Auth0 API audience (must match backend API configuration)
AUTH0_AUDIENCE=https://api.virtualstaging.local

# Auth0 OAuth scopes
AUTH0_SCOPE=openid profile email

# Secret for encrypting session cookies (generate with: openssl rand -hex 32)
AUTH0_SECRET=your_secret_key_at_least_32_characters_long

# Application base URL
APP_BASE_URL=http://localhost:3000

# Next.js API base URL (for proxying to backend)
NEXT_PUBLIC_API_BASE=/api

# Important: Add these URLs to your Auth0 Application settings:
# - Allowed Callback URLs: http://localhost:3000/auth/callback
# - Allowed Logout URLs: http://localhost:3000
# - Allowed Web Origins: http://localhost:3000
