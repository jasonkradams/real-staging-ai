# API secrets file example
# Copy this to secrets.yml and fill in your actual secrets
# 
# IMPORTANT: secrets.yml must be encrypted with HashiCorp Vault before committing
# This file is gitignored and will not be committed
#
# To encrypt: vault encrypt secrets.yml
# To decrypt: vault decrypt secrets.yml

# Currently no secrets are required for the API service
# Add secrets here as needed
