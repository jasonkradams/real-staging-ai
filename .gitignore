# Go coverage files
coverage.html
coverage.*.out
coverage.out

# NPM Test Coverage
coverage/

.localstack/
.env*

# Local secrets (do not commit)
secrets.yml
.secrets/

# Local config overrides (do not commit)
config/local-override.yml
config/*.local.yml
tmp
